# Commands

```
npx sequelize-cli model:create --name Product --attributes sourceUrl:string,coupon:text,images:text,price:string,rating:string,title:string,orderCount:string,posted:boolean,affiliateUrl:string --force

npx sequelize-cli db:migrate

```